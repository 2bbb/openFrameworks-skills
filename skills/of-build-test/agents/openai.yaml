interface:
  display_name: "oF Build/Test"
  short_description: "Build, run, log, and visually verify openFrameworks projects."
  default_prompt: "Use $of-build-test to build, run, capture logs, and verify this openFrameworks app or addon test project."
