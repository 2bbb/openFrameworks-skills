interface:
  display_name: "oF Build/Test"
  short_description: "Build, test, benchmark, and visually verify oF projects"
  default_prompt: "Use $of-build-test to select layered checks, build and run this openFrameworks project, capture logs/artifacts, and produce evidence-backed verification."
