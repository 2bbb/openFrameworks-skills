# Prompt: author or review an openFrameworks addon

Use `of-addon-authoring`, `of-platform-config`, and `of-openframeworks`. Check addon layout, `addon_config.mk`, examples, tests, platform sections, dependency declarations, and generated project behavior against bundled addon patterns before editing.
