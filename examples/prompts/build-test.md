# Prompt: build and smoke-test an oF project

Use `of-build-test` plus `of-openframeworks`. Locate `OF_ROOT`, inspect `addons.make`, `config.make`, and generated files, then run the smallest relevant build/test command. If no executable smoke test is safe, explain the verified gap and next best check.
