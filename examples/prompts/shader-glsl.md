# Prompt: debug an oF shader

Use `of-shader-glsl` and `of-openframeworks`. Inspect the target renderer, shader example nearest to the feature, texture target assumptions, uniforms, matrices, and asset paths. Patch only source-backed issues and verify with build or the smallest available runtime check.
